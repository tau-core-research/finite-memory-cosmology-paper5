# Reconstruction-Family Candidate Exports

This directory is reserved for future public source-split reconstruction-family
response exports.

Expected future file:

```text
source_split_reconstruction_family_responses.csv
```

That file is intentionally absent until the public candidate export is actually
prepared. Do not copy the non-scoring preview from `evidence/` into this
directory as a shortcut. The preview is schema plumbing only and does not
authorize K2 scoring.
