# P-TauCov Reduced Branch-Jacobian Requirement

Freeze ID: `P_TAUCOV_REDUCED_BRANCH_JACOBIAN_REQUIREMENT_v1`

Status:

`P_TAUCOV_REDUCED_BRANCH_JACOBIAN_REQUIRED_NO_OBJECT_NO_SCORING`

## Motivation

The Q-range retest shows that the minimal branch coordinate contrast is almost
entirely outside the common clean subspace:

```text
QRangeRetention(B_BRANCH_RESPONSE - PHI_PARENT_SOURCE) = 9.11401163254385e-16
```

Therefore the next P-TauCov branch-response candidate cannot be:

```text
v_branch = B - Phi
```

It must be derived from the reduced branch equation.

## Required Object

The next valid route must build:

```text
J_response = Q_range D_B M_proj (L_B^red)^(-1) D_Phi F_B
K_Q = J_response J_response^T
```

The operators must be declared before scoring:

| Operator | Required role |
|---|---|
| `L_B^red` | reduced branch Hessian / response operator after forbidden directions are removed |
| `D_Phi F_B` | derivative of the branch equation under parent perturbation |
| `D_B M_proj` | derivative of projected morphology/readout with respect to branch state |
| `Q_range` | frozen orthogonal clean-subspace projector |

## Why This Is Different

The failed candidate used only coordinate contrast. The required object uses
the actual response pathway:

```text
parent perturbation -> branch equation -> branch response -> projected readout -> clean subspace
```

This is closer to the Tau-side claim because the covariance object would be
generated by branch response, not by a hand-picked covariance shape.

## Next Artifact

The next no-scoring artifact should be:

```text
docs/p_taucov_reduced_branch_jacobian_source_spec.md
scripts/build_p_taucov_reduced_branch_jacobian_source_spec.py
scripts/validate_p_taucov_reduced_branch_jacobian_source_spec.py
```

It should define the operator sources and blockers only. It should not build a
scorecard.

## Claim Boundary

Allowed statement:

> The Q-range retest shows that P-TauCov now requires a reduced branch-Jacobian source, not a simple branch-coordinate contrast.

Forbidden statement:

> The reduced branch-Jacobian requirement validates Tau Core, constructs a scoreable object, or authorizes empirical scoring.
