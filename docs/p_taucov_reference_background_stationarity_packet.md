# P-TauCov Reference Background Stationarity Packet

Status: stationary reference background / stability conditional on `S_rest` /
no empirical scorecard / no survival claim.

The full-action packet still has a `REFERENCE_BACKGROUND` blocker. The active
projection-essential scaffold admits a natural local reference point:

```math
\Phi_0 = 0,\quad P_0 = 0,\quad B_0 = 0 .
```

For the active scaffold sector

```math
V_{\rm active}
=
-\frac{1}{2}B^2 - 2PB - P\Phi ,
```

the first derivatives at this point are:

```math
\partial_B V_{\rm active} = -B - 2P = 0,
```

```math
\partial_P V_{\rm active} = -2B - \Phi = 0,
```

```math
\partial_\Phi V_{\rm active} = -P = 0.
```

Thus the origin is a stationary reference background for the active scaffold.

## Stability Boundary

This packet does not prove full stability. The active scaffold is a witness
sector, not a complete positive energy functional. Stability must be supplied
by the remaining `S_rest` embedding. Therefore this packet resolves
stationarity but not full background stability.

## Claim Boundary

Allowed:

```text
The active scaffold has a declared stationary reference background at
Phi=P=B=0.
```

Forbidden:

```text
The full Tau Core background is stable, or the empirical covariance signal is
validated.
```
