# P-TauCov P3 Core-Mixing Operator

Status: target-blind core-mixing projection operator / no updated model packet / no
empirical scoring authorization.

The P2 operator fixed the P1 null-separation failure, but its covariance
response remained too narrowly localized and failed the support-entropy gate.
This P3 packet freezes a support-widening core-mixing structure without using
target residuals, P5C v3 outcomes, score behavior, or family-localized tuning.

## Frozen Construction

```text
all ordered off-diagonal links among:
  parent source
  branch response
  parent morphology
  morphology projection
```

with unit Frobenius normalization:

```text
each nonzero link = 1/sqrt(12)
```

The four-axis core is deliberately restricted to Tau-side coordinates and
excludes coordinate-convention and external-metadata axes. The design directly
targets the P2 support-entropy failure while preserving the no-outcome,
no-residual, no-score construction rule.

## Claim Boundary

Allowed statement:

```text
A target-blind P3 core-mixing operator is frozen for later specificity
auditing.
```

Forbidden statement:

```text
The P3 operator has produced a Tau signal, passed empirical scoring, or
authorized P-TauCov survival claims.
```
