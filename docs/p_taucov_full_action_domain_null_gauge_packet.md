# P-TauCov Full-Action Domain And Null/Gauge Packet

Status: full-action embedding domain packet / no full action / no empirical
scorecard / no survival claim.

The earlier reference-domain route treated the projection coordinate as a
gauge-like removable direction for some linear packets. The projection-essential
scaffold has a different role for `P`: it is an active mediator. Therefore the
full-action embedding requires its own active domain.

## Declared Parent Domain

The current parent domain is a finite eight-coordinate tau response cell:

```text
Phi, B, M, P, coordinate-origin, coordinate-scale, external-family,
external-observing-context
```

For the projection-essential active sector, the embedded active domain is:

```text
Phi, B, P
```

The excluded complement is:

```text
M, coordinate-origin, coordinate-scale, external-family,
external-observing-context
```

## Null/Gauge/Forbidden Policy

| Subspace | Coordinates | Reason |
| --- | --- | --- |
| active | `Phi`, `B`, `P` | source, branch, and projection mediator are required by the scaffold |
| null | none | no exact zero mode is declared in the active scaffold |
| gauge | coordinate-origin, coordinate-scale | coordinate-convention directions |
| forbidden | `M`, external-family, external-observing-context | morphology/null competitor and external metadata cannot enter the witness sector |

This policy resolves two embedding blockers:

```text
PARENT_DOMAIN
NULL_GAUGE_MODES
```

It does not resolve:

```text
REFERENCE_BACKGROUND
S_REST
COVARIANCE_MAP
```

## Claim Boundary

Allowed:

```text
The full-action embedding now has a declared finite parent domain and
null/gauge/forbidden policy for the projection-essential scaffold.
```

Forbidden:

```text
The full parent action is complete or the covariance signal is validated.
```
