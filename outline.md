# Outline

## 1. Introduction

Motivate finite-memory projection corrections as a diagnostic complement to
FLRW consistency, Dyer-Roeder optical effects, and cosmic-backreaction tests.

Claim boundary: no observational detection claim.

## 2. Finite-Memory K2 Operator

Define normalized projected depth `x` and the effective response operator:

```text
W(x) = 1 + rho*x^3
```

Derive the passive memory bound:

```text
Integral_0^1 rho*x^3 dx <= Integral_0^1 1 dx
rho <= 4
```

Explain the kernel-family audit and why the cubic shape is selected.

Claim boundary: effective operator, not full global Tau action theorem.

## 3. Diagnostic Targets

Define reconstructed BAO-only and SN+BAO diagnostic targets. Explain why
source-split and method-family reconstructions are diagnostics, not direct
published likelihoods.

Introduce the sign-stability policy:

```text
sign-stable reconstructed point   -> exact sign gate
sign-unstable reconstructed point -> envelope / diagonal residual gate
```

## 4. Results

Report:

- BAO-only full diagnostic overlap;
- strict SN+BAO sign tension;
- localization of the `z=1.1925` dip;
- sign-stability-aware SN+BAO non-violation status.

## 5. Limitations And Next Likelihood

State that the current work is a theory-method diagnostic paper. The next
observational paper requires a full covariance or shrinkage-likelihood
comparison against direct published likelihood products.
